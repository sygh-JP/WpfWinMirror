<UserControl
    x:Class="MyWpfCtrls.MetroIconBurger"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    mc:Ignorable="d" 
    Name="userCtrl"
    IsHitTestVisible="False"
    >
    <Grid Width="16" Height="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="2"/>
            <RowDefinition Height="4"/>
            <RowDefinition Height="2"/>
            <RowDefinition Height="4"/>
            <RowDefinition Height="2"/>
        </Grid.RowDefinitions>
        <Rectangle Grid.Row="0" Fill="{Binding ElementName=userCtrl, Path=Foreground}" SnapsToDevicePixels="True"/>
        <Rectangle Grid.Row="2" Fill="{Binding ElementName=userCtrl, Path=Foreground}" SnapsToDevicePixels="True"/>
        <Rectangle Grid.Row="4" Fill="{Binding ElementName=userCtrl, Path=Foreground}" SnapsToDevicePixels="True"/>
    </Grid>
</UserControl>
